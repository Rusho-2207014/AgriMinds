# AgriMinds Database Configuration
# MySQL Connection Settings

# Database URL (update with your MySQL server details)
db.url=jdbc:mysql://localhost:3306/agriminds_db?useSSL=false&serverTimezone=Asia/Dhaka&allowPublicKeyRetrieval=true
db.username=root
db.password=Rushorkr@gmail.com

# Connection Pool Settings
db.pool.maxPoolSize=10
db.pool.minIdle=2
db.pool.connectionTimeout=30000

# API Keys (Replace with actual keys)
weather.api.key=your_weather_api_key_here
weather.api.url=https://api.weatherapi.com/v1

sms.api.key=your_sms_api_key_here
sms.api.url=https://api.smsgateway.bd/send
sms.sender.id=AgriMinds

# Email Configuration (for notifications)
email.smtp.host=smtp.gmail.com
email.smtp.port=587
email.username=your_email@gmail.com
email.password=your_app_password
email.from=noreply@agriminds.com

# Application Settings
app.name=AgriMinds
app.version=1.0.0
app.language.default=Bengali

# Logging
logging.level=INFO
logging.file=logs/agriminds.log
